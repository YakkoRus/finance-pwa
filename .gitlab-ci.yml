pages:
  stage: deploy
  script:
    - echo "Deploying to GitLab Pages"
  artifacts:
    paths:
      - public
  only:
    - master

before_script:
  - mkdir -p public
  - cp -r index.html manifest.json sw.js css js public/